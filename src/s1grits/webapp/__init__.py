"""S1-GRiTS web interface (v2.3).

API-first FastAPI server over a processing workspace, plus a zero-build
static frontend. Install the optional extra to use it:

    pip install "s1grits[web]"
    s1grits serve --root /path/to/outputs

See docs/webapp_v2_architecture.md (strategy) and docs/webapp.md (reference).
"""
