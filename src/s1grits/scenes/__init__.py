"""Scenes/smonthly writer machinery (blockwise blocks, windowed mosaic
readers, COG export, STAC items). workflow_scenes re-exports the public
surface; import from there unless you need a submodule directly."""
